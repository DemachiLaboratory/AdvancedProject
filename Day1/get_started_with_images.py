import numpy as np
import cv2

## TODO
# 画像を読み込む
img = 

# 画像を表示する

# キーボード入力を処理する
k = 
if k == 27: # wait for ESC key to exit
    # 現在までに作られた全てのウィンドウを閉じる
    
elif k == ord('s'): # wait for 's' key to save and exit
    # 画像を保存する
    
    # 現在までに作られた全てのウィンドウを閉じる
    
## TODO